#!/usr/bin/env python
from setuptools import setup, find_packages
setup(
    name='decision_trees',
    version='0.1',
    description='Decison trees from scratch',
    packages=find_packages(include=['decision_trees']),
    install_requires=[
    ],
)
